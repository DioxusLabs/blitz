# wasm_7guis

A WASM implementation of the [7GUIs](https://7guis.github.io/7guis/) benchmark tasks, rendered by **dioxus-native** using the `vello-hybrid` (WebGL) renderer.

## Build

```sh
trunk build --release
trunk serve --release
```
